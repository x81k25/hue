[project]
name = "hue"
version = "0.1.0"
description = "Hue schedule manager — FastAPI service + Streamlit UI"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "httpx>=0.27",
    "pydantic>=2.6",
    "streamlit>=1.35.0",
    "pandas>=2.0",
    "requests>=2.31.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",
]
